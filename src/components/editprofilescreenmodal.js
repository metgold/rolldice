/* import { Modal, Portal, Provider } from 'react-native-paper';
import EditProfileScreeen from '../features/account/screens/editprofile.screen';
import React, {useContext} from 'react'
import ModalContext from './utility/modalcontext';


export const EditProfileModal = () => {
    const { visible, hideModal, edit, withdraw, handlePress, privacy, password, signout} = useContext(ModalContext);
    <Provider>
            <Portal>
                <Modal visible={visible} onDismiss={hideModal}>
                
                 <EditProfileScreeen handlePress= {() => handlePress()} />
                 
                    
                </Modal>
              </Portal>
</Provider>
} */